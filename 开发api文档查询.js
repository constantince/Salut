/**
 * 获取某个模块对象
 * {param: string (module's name)}
 */
PDW.Observer.get(moduleName)

/**
 * 跳转到某个路由的界面下去
 * {param:routeName路由名, callback:跳转路由后的回调函数}
 */
router.myNavigate(routeName, callback)

/**
 * 将数据传入某个界面
 * param: obj json数据
 */
Page.addDataToModel(obj);

/**
 * 刷新某个界面
 */
this.children['h'].reloadView({
  word: 'I AM OTERHS INFOMATION FORM PAGE A'
});